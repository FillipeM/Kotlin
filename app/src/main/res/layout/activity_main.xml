<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <Button
        android:id="@+id/btnMenuUsuario"
        android:textSize="32sp"
        android:text="Usuários"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>
    <Button
        android:id="@+id/btnMenuCategoria"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="32sp"
        android:text="Categorias"
        app:layout_constraintEnd_toEndOf="@+id/btnMenuUsuario"
        app:layout_constraintStart_toStartOf="@+id/btnMenuUsuario"
        app:layout_constraintTop_toBottomOf="@+id/btnMenuUsuario"/>
    <Button
        android:id="@+id/btnMenuProduto"
        android:textSize="32sp"
        android:text="Produtos"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="@+id/btnMenuCategoria"
        app:layout_constraintEnd_toEndOf="@id/btnMenuCategoria"
        app:layout_constraintTop_toBottomOf="@id/btnMenuCategoria"/>
</androidx.constraintlayout.widget.ConstraintLayout>